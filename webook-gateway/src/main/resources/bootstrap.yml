spring:
  application:
    name: webook-gateway # 应用名称
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true # 启用服务发现
        group: DEFAULT_GROUP # 所属组
        namespace: webook # 命名空间
        server-addr: 110.41.141.141:8848 # Nacos 服务器地址